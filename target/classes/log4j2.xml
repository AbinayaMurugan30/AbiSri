<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="5">
    <Properties>
        <Property name="logDir">logs</Property>
    </Properties>

    <Appenders>

        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n" />
        </Console>

        <!-- Routing Appender for scenario-specific logs -->
        <Routing name="ScenarioRouting">
            <Routes pattern="$${ctx:scenarioName:-Thread-${ctx:threadId}}">
                <Route>
                    <File name="Scenario-${ctx:scenarioName}" fileName="${logDir}/${ctx:scenarioName}.log" append="true">
                        <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5p %c - %m%n" />
                    </File>
                </Route>
            </Routes>
        </Routing>

    </Appenders>

    <Loggers>
        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="ScenarioRouting"/>
        </Root>
    </Loggers>
</Configuration>
